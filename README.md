# testingWSL — Hardening changes (austria-admission-head)

This repository contains a focused hardening pass for the `austria-admission-head` module. See the module-specific summary in `austria-admission-head/README-changes.md` for the full list of changed files, why they were changed, and the impact.

Quick pointers

- Branch with all changes: `hardening/austria-admission-head`
- Module summary: `austria-admission-head/README-changes.md`
- Draft PR: `PR_DRAFT_hardening_austria.md`

Why this README exists here

The change set is currently on the `hardening/austria-admission-head` branch. Adding this root `README.md` makes the repository show a descriptive readme when you view the branch or after the PR is merged into `main`.

Next steps

- Open the PR and merge into `main` to make these changes visible on the repository homepage.
- Alternatively, view the branch directly in GitHub using the branch switcher.
